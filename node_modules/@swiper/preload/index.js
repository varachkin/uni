/*
 * This file sets up some basic, universal error handling and then
 * attempts to determine if things are being run from within the
 * swiper monorepo.  If it is running from within the monorepo then it
 * sets up universal-resolver and babel to do the right thing for
 * a development environment.
 */

/*
require( 'log-process-errors' )( {
  exitOn  : [ 'uncaughtException', 'unhandledRejection' ],
  colors  : true,
} );
*/

// Everything after this point is removed from a production build, so
// the code that follows this only applies in development.
